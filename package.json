{
  "name": "academic-pinpoint",
  "private": true,
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "db:setup": "pnpm --filter @academic-pinpoint/api db:setup"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
